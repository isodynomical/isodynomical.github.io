ToDo:
Add esc as an function to clear the enlarged images
Make it so when you click on one gallery box others close

For Update:
git add -A
git commit -m "change message (be specific)"
git push

For New:
git init
git add -A
git commit -m "first commit"
=> Copy/Paste line from github (git remote add origin...)
git checkout -b gh-pages  // If hosting a website
git push -u origin gh-pages // If hosting.  If not, gh-pages = master

noStroke();

// position of the car
var x = 11;

var draw = function() {
    background(252, 255, 214);
    // draw the car body
    fill(255, 0, 115);
    rect(x, 200, 100, 20);
    rect(x + 15, 178, 70, 40);
    
    // draw the wheels
    fill(77, 66, 66);
    ellipse(x + 25, 221, 24, 24);
    ellipse(x + 75, 221, 24, 24);
    
    x = x + 10;
};